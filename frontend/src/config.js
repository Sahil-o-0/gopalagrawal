import Constants from 'expo-constants';

// For local testing, we fallback to our local LAN IP. For production cloud deployments,
// we route all API requests to the unified domain address: aadarshila.demosite.cloud.
export const API_BASE_URL = 'https://aadarshila.demosite.cloud/api';
